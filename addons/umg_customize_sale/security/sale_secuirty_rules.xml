<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- OVERRIDE ON DEFAULT RULES -->

    <!-- <record id="group_sale_salesman_bu_all_leads" model="res.groups">
        <field name="name">User: All Documents (Business)</field>
        <field name="category_id" ref="base.module_category_sales_sales"/>
        <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        <field name="comment">the user will have access to all records of everyone in the sales application.</field>
    </record>

    <record id="bu_sale_order_see_all" model="ir.rule">
        <field name="name">All Orders Of Related Business</field>
        <field ref="model_sale_order" name="model_id"/>
        <field name="domain_force">['|', ('hr_br_id', '=',user.current_bu_br_id.id), ('hr_bu_id', '=',user.current_bu_br_id.id)]</field>
        <field name="groups" eval="[(4, ref('umg_customize_sale.group_sale_salesman_bu_all_leads'))]"/>
    </record>

    <record id="bu_sale_order_line_see_all" model="ir.rule">
        <field name="name">All Orders Lines Of Related Business</field>
        <field ref="model_sale_order_line" name="model_id"/>
        <field name="domain_force">[('hr_bu_id', '=',user.current_bu_br_id.id)]</field>
        <field name="groups" eval="[(4, ref('umg_customize_sale.group_sale_salesman_bu_all_leads'))]"/>
    </record>

    <record id="sales_team.group_sale_salesman_all_leads" model="res.groups">
        <field name="name">User: All Documents</field>
        <field name="category_id" ref="base.module_category_sales_sales"/>
        <field name="implied_ids" eval="[(4, ref('umg_customize_sale.group_sale_salesman_bu_all_leads'))]"/>
        <field name="comment">the user will have access to all records of everyone in the sales application.</field>
    </record> -->

</odoo>
