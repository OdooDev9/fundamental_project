<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_stock_return_picking_form_inherit" model="ir.ui.view">
    <field name="name">stock.return.picking.stock.account.form</field>
    <field name="inherit_id" ref="stock.view_stock_return_picking_form"/>
    <field name="model">stock.return.picking</field>
    <field name="arch" type="xml">
        <xpath expr="//field[@name='location_id']" position="after">
            <field name="reason_required" invisible="1"/>
            <field name="borrow_to_date" decoration-danger="1" readonly="1"/>
            <field name="return_reason" attrs="{'required':[('reason_required','=',True)]}"/>
        </xpath>
    </field>
    </record>

</odoo>
